{
  "name": "campable-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end test suite for campable.co — covers monetization flows (signup → upgrade, watch/planner limits, cancel/reactivate).",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/upgrade.spec.ts",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
